// Feature flags for controlling site functionality
// Toggle these to enable/disable features across the entire site

export const FEATURES = {
  // Discovery Set product - set to true when ready to launch
  DISCOVERY_SET_ENABLED: false,
};
